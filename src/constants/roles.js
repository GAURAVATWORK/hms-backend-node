const User_Roles = {
    PATIENT: "PATIENT",
    DOCTOR: "DOCTOR",
    ADMIN: "ADMIN"
};

const PUBLIC_SIGNUP_ROLES = [
    User_Roles.PATIENT,
    User_Roles.DOCTOR
];

export {
    PUBLIC_SIGNUP_ROLES
};

export default User_Roles;